**/bin/
**/obj/
**/*.user
**/TestResults/
WeatherDashboard.Tests/

# VCS / editors
**/.git/
**/.vscode/
**/.idea/

# Not part of a .NET restore/build — defensive in case any project dir ever gets one
**/node_modules/

# Local environment / secrets — none should exist under backend/, but never send them if they do
**/.env
**/.env.*
**/*.pfx
**/*.pem
**/*.key
